<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rusbumhelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu_Exemption_Activity" />
        <activity android:name=".Selection_name_activity" />
        <activity android:name=".Text_Facil_View_Activity" />
        <activity android:name=".Autoriz_Activity" />
        <activity android:name=".SignIn" />
        <activity android:name=".Registration" />
    </application>

</manifest>